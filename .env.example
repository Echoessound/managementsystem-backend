# ============================================================
# 数据库
# ============================================================
DATABASE_URL=mongodb://localhost:27017/ctrip_hotel
DB_NAME=ctrip_hotel

# ============================================================
# JWT 认证
# ============================================================
JWT_SECRET=your-secret-key-change-in-production

# ============================================================
# AI 服务（可选）
# ============================================================
AI_API_KEY=sk-your-api-key
AI_BASE_URL=https://api.openai.com
AI_MODEL=gpt-3.5-turbo

# ============================================================
# 服务器
# ============================================================
PORT=8080

# ============================================================
# 性能监控与慢查询（可选）
# ============================================================
# 慢查询阈值（毫秒），超过此时间的数据库操作会被记录到 SlowQueryLog 集合
# 默认: 100
SLOW_QUERY_THRESHOLD=100

# 慢响应阈值（毫秒），超过此时间的 HTTP 请求会被记录到 SlowQueryLog 集合
# 默认: 500
SLOW_RESPONSE_THRESHOLD=500

# ============================================================
# 备份加密（可选）
# ============================================================
# AES-256-GCM 加密密钥（64 位十六进制字符串，即 32 字节）
# 生成方法（Node.js）: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 不配置此变量则备份不加密（向后兼容）
# BACKUP_ENCRYPT_KEY=
